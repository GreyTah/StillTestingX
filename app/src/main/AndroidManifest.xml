<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="m.inschool1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".aMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bScheduleActivity"
                  android:label="Schedule"
                  android:theme="@style/AppTheme"/>
        <activity android:name=".cCalendarActivity"
                  android:label="Calendar"
                  android:theme="@style/AppTheme"/>
        <activity android:name=".dFaltasActivity"
                  android:label="Faltas"
                  android:theme="@style/AppTheme"/>
        <activity android:name=".eMediasActivity"
                  android:label="Medias"
                  android:theme="@style/AppTheme"/>
        <activity android:name=".gSettingsActivity"
                  android:label="Settings"
                  android:theme="@style/AppTheme"/>
        <activity android:name=".ePhotosActivity"
                  android:label="Photos"
                  android:theme="@style/AppTheme"></activity>
    </application>

</manifest>
